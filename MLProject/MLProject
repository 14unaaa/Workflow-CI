name: Workflow-CI-Project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      data_path: {type: string, default: "getcontact_preprocessing.csv"}
    command: "python modelling.py --data_path {data_path}"
